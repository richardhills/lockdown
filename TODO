1. Make sure that every jump() call is validated against the get_break_types() values it returned earlier
2. Get a dump of the AST as JSON for investigating inferred values
3. Get a printout of the code with all inferred values marked as such